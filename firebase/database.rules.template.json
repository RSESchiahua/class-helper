{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid && auth.uid === 'YOUR_FIREBASE_UID'",
        ".write": "auth != null && auth.uid === $uid && auth.uid === 'YOUR_FIREBASE_UID'"
      }
    }
  }
}
